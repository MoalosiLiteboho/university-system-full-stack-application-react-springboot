server:
    port: 5000

spring:
    application:
      name: authority
    datasource:
        url: jdbc:mysql://localhost:3306/university_database
        username: 'root'
        password: ''
    jpa:
        hibernate:
            ddl-auto: update
        show-sql: 'true'
        properties:
            hibernate:
                format_sql: 'true'
#                dialect: org.hibernate.dialect.MySQL5Dialect

